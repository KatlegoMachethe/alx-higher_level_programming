-- This script computes the average of all record in 'second_table' in 'hbtn_0c_0'.
-- The results column name name should be "average".
-- The database name is passed as an argument in the terminal.

SELECT AVG(score) AS average
FROM second_table;

