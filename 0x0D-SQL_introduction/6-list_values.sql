-- This scripts lists all rows on the table first_table from the hbtn_0c_0 database.
-- All fields should be printed
-- The database name will be passed as an argument to the mysql commans.

SELECT * FROM first_table;
