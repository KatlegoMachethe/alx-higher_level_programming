-- This script displays the number of record with id=89 int the first_table of hbtn_0c_0 database
-- Database name will be passed as argument in the terminal.

SELECT COUNT(id)
FROM first_table
WHERE id = 89;
