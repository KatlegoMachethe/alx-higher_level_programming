#!/usr/bin/node
/*
 * uses for loop to print three lines in console
 */

const languages = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
let i = 0;

for (; i < languages.length; i++) {
  console.log(languages[i]);
}
